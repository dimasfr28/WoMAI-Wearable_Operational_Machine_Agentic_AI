# Deliberately mineru[pipeline], NOT mineru[all]/mineru[vlm]/mineru[vllm] — those
# pull in vLLM + the full CUDA stack, multi-GB and useless without a GPU. Kept in
# sync with the version this service was validated against; bump deliberately.
mineru[pipeline]==3.4.4
